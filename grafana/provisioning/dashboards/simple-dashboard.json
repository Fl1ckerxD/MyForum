{
    "dashboard": {
        "title": "MyForum Simple Dashboard",
        "panels": [
        {
            "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
            "id": 1,
            "title": "HTTP Requests Rate",
            "type": "graph",
            "targets": [
            {
                "expr": "rate(http_server_request_duration_seconds_bucket[5m])",
                "legendFormat": "{{handler}}"
            }
            ]
        }
        ],
        "schemaVersion": 27,
        "tags": ["myforum"],
        "timezone": "browser"
    },
    "overwrite": true
}
